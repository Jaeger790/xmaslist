# render.yaml
services:
  # Backend API
  - type: web
    name: xmaslist-api
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    region: oregon

  # Frontend Static Site
  - type: web
    name: xmaslist
    env: static
    buildCommand: echo "No build needed"
    startCommand: echo "Serving static"
    staticPublishPath: ./static
    plan: free
    region: oregon
    routes:
      - type: rewrite
        source: /api/**
        destination: https://xmaslist-api.onrender.com/**
